# Simple example of Linked HTML library

This repository is a simple example created for presentation about
[Linked HTML](https://github.com/smalluban/linked-html).

To start application run:
```
npm install && npm start
```
