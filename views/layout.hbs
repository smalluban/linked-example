<!DOCTYPE html>
<html>
  <head>
      <meta charset="utf-8">
      <title>Example App</title>
      <style>
        html, body {
          font: 20px sans-serif;
        }
        h1, h3, p { margin-top: 0; }
        .results { padding-top: 20px; }
        .product { padding: 5px; }
        .product:nth-child(odd) { background: #efefef; }
        .product .info { display: flex; }
        .product .info img {
          display: block;
          height: 100px;
          width: 150px;
          flex: 0 0 auto;
          margin-right: 10px;
        }
      </style>
  </head>
  <body>
      {{{body}}}
      <script src="js/app.js"></script>
  </body>
</html>
